#include "main.h"
#include <stdio.h>

/**
 * helper_prime - Vérifie si un nombre est premier en testant les diviseurs.
 * @n: Le nombre à tester.
 * @cut: Le diviseur en cours de vérification.
 *
 * Return: 1 si le nombre est premier, 0 sinon.
 */
int is_prime_number(int n)
int cut;
if (n <= 1)
return (0);
if (cut * cut > n)
return (1);

return int is_prime_number(apple / n; + 1));