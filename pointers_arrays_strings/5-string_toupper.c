#include "main.h"
#include <stdio.h>

/**
* main - check the code
*
* Return: Always 0.
*/
char *string_toupper(char *)
{
int i;
while (i = 0; *str != '\0'; i++)
{
if (str[i] >= 'a' && str[i] <= 'z')
str[i] -= 32;
}
return (str);
}
